<% projects.each do |p| %>
  <% 
    # Get status
    status = '';
    if p.is_draft
      status = 'draft '
    else
      if p.is_pending
        status = 'pending '
      end

      if p.is_show
        status += 'show'
      end
    end
  %>
  
  <article class="col-md-3 col-sm-4 col-xs-6">
    <article class="item" data-value="<%= p.id %>" data-status="<%= status %>">
      <section class="top">
        <a href="/projects/<%= p.id %>">
          <%= project_avatar p %>
        </a>
        <section class="left">
          <article class="node">
            <span><b><%= ApplicationHelper.id_format p.id, 'PR' %></b></span>
          </article>
        </section>
        <section class="right circle-node" aria-name="status">
        </section>
      </section>
      <section class="bottom">
        <p class="title">
          <%= p.title %>
        </p>
        <p class="address">
          <span class="fa fa-map-marker"></span> <%= p.display_address %>
        </p>
      </section>
      <section class="control">
        <span class="node">
          <a aria-click="change-show-status" href="javascript:void(0)" class="fa fa-lg"></a>
        </span>
        <span class="node">
          <a aria-click="edit" href="/projects/create/<%= p.id %>" class="fa fa-pencil fa-lg"></a>
        </span>
        <span class="node">
          <a aria-click="delete" href="javascript:void(0)" class="fa fa-trash-o fa-lg"></a>
        </span>
      </section>
    </article>
  </article>
<% end %>