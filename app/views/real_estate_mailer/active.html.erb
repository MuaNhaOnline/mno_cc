<h3>Chào <b><%= @re.user_full_name %></b>, chúc mừng bạn đã đăng tin thành công tại MuaNhaOnline.vn</h3>

<% 
	query = { secure_code: @re.params['secure_code'] }.to_query
%>

<ol>
	<li>
		Kích hoạt tin tại <a href="<%= Rails.application.config.base_url %>bat-dong-san/active/<%= @re.id %>?<%= query %>">đây</a>.
	</li>
	<li>
		Xem tin tại <a href="<%= Rails.application.config.base_url %>bat-dong-san/<%= @re.full_slug %>?<%= query %>">đây</a>.
	</li>
	<li>
		Sửa tin tại <a href="<%= Rails.application.config.base_url %>bat-dong-san/chinh-sua/<%= @re.id %>?<%= query %>">đây</a>.
	</li>
	<li>
		Xóa tin tại <a href="<%= Rails.application.config.base_url %>bat-dong-san/delete/<%= @re.id %>?<%= query %>">đây</a>.
	</li>
</ol>
