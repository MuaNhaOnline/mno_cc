<%= form_for(@disadvantage) do |f| %>
  <% if @disadvantage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@disadvantage.errors.count, "error") %> prohibited this disadvantage from being saved:</h2>

      <ul>
      <% @disadvantage.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </div>
  <div class="field">
    <%= f.label :code %><br>
    <%= f.text_area :code %>
  </div>
  <div class="field">
    <%= f.label :options %><br>
    <%= f.text_area :options %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
