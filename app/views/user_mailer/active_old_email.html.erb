<h3>Chào <b><%= @user.full_name %></b>, email của bạn vừa được thay đổi tại hệ thống MuaNhaOnline.vn</h3>
<%
	url = "#{Rails.application.config.base_url}users/active_account/#{@user.id}?#{{ code: @user.params['active_code'] }.to_query}"
%>
<p>Để xác nhận thay đổi hãy nhấn vào <a href="<%= url %>">đây</a>.</p>
<p><small><a href="<%= url %>"><%= url %></a></small></p>