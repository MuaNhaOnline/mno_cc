<%= form_for(@property_utility) do |f| %>
  <% if @property_utility.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@property_utility.errors.count, "error") %> prohibited this property_utility from being saved:</h2>

      <ul>
      <% @property_utility.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </div>
  <div class="field">
    <%= f.label :code %><br>
    <%= f.text_area :code %>
  </div>
  <div class="field">
    <%= f.label :options %><br>
    <%= f.text_area :options %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
